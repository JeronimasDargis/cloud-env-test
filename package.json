{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "start": "nuxt start",
    "create-env": "printenv > .env"
  },
  "dependencies": {
    "@rcco/gum": "^4.0.0-alpha.5",
    "dotenv": "^16.0.3",
    "nuxt": "^3.3.2"
  }
}
